# Goodreads User Scraper

A simple tool to scrape a users profile page from goodreads, it achieves the same result as when you export your own library.

The tool then exports this to a csv file for you to import into your own library.

You can specify which bookshelves to export.